let firstValue = true;
let secondValue = 0;

// console.log(firstValue == secondValue); // == - это сравнение переменных с приведением типов к одному
// console.log(firstValue === secondValue); // === - это строгое сравнение переменных (ошибка при разных типах данных)


let variableWithNull = null; // Переменная содержит только Null
let variableUndefined; // Переменная не определена, так как ей не присвоено ничего.

console.log(variableWithNull);
console.log(variableUndefined);


// node 2_BooleanNullDefined.js // FOR RUN